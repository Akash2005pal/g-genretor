var express = require('express');

const e = require('express');
var router = express.Router();


/* GET home page. */
router.get('/log_interface', function (req, res, next) {
  res.render('login', );
});
module.exports = router;
